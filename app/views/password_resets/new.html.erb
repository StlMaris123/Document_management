<% provide(:title, "Forgot Password") %>
<h1>Forgot Password</h1>
<div class="row">
    <div class="medium-6 columns medium-offset-3">
        <%= form_for(:password_reset, url: password_resets_path) do |f| %>
            <%= f.label :email %>
            <%= f.email_field :email %>
            <%= f.submit "Submit", class: "button large primary" %>
        <% end %>
    </div>
</div>
